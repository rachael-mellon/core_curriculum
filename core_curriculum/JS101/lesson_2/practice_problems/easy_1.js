// 1) This will not raise an error. It will create a new index in the array
// and the element at that index will be 5. Each index between 3 and 5 will
// return undefined
// 2) use the String.endsWith() function with '!' in the parenthesis
// 3) ages.hasOwnPropert('Spot');
// 4) munstersDescription.charAt(0).toUpperCase() +
// munstersDescription.substring(1).toLowerCase();
// 5) true, false
// 6) Object.assign(ages, additional ages)
// 7) str1.includes('Dino');
// str2.includes('Dino'); 
// 8) flinstones.push('Dino');
// 9) flinstones.push('Dino', 'Hoppy');
// 10) let advice = "Few things in life are as important as house training your pet dinosaur.";
// advice.slice(0, advice.indexOf('house'));